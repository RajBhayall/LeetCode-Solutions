class Solution {
public:
    int rob(vector<int>& nums) {
        int one=0, two=0, temp =0;
        for(int num : nums){
            temp=one;
            one = max(one, two+num);
            two=temp;
        }
        return one;
    }
};
