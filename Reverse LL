class Solution {
public:
    ListNode* reverseList(ListNode* head) {
        ListNode *temp = head, *ptr=NULL, *help;
        while(temp != NULL){
            help=temp->next;
            temp->next=ptr;
            ptr=temp;
            temp=help;
        }
        head=ptr;

        return head;
    }
};
