class Solution {
public:
    int findKthLargest(vector<int>& nums, int k) {  // 1 2 3 4 5 6
        sort(nums.begin(), nums.end()); 
        int pos;
        int n=nums.size();
        for(int i=n-1; i>=n-k; i--){
            pos = i;
        }
        return nums[pos];
    }
};  
